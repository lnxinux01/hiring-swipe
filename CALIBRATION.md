# 画像推断的标定记录

卡片是 5 个维度的**正交全组合**（4×2×3×3×3 = 216 组，每组 2 个变体 = 432 张）。
第 6 个维度「学校」**随机分配、不进网格** —— 它是对照变量：本不该出现任何规律，
如果它照样跑出信号，说明面试官在无意识按学校筛人。

## 推断链路

1. **主动选卡**（`nextCard`）前 5 张求维度差异最大做粗定位；之后按
   `(0.35 + 该维度当前效应) / (1 + 该取值已曝光次数)` 打分挑下一张，
   并乘 0.8~1.2 的随机抖动 —— 纯 argmax 会在小样本里制造维度间伪相关，反过来骗过回归。
   实测曝光可以做到完全均衡（n=24 时每个取值 6 次）。

2. **岭回归 logistic**（`fitRidge`, λ=0.015）把 6 个维度一起放进模型。
   只用边际统计（"右滑的人里 80% 有 0-1 经历"）会被混淆：
   实测中纯噪音的「学校」维度可以排到第 1。多维同时拟合只给一个维度
   它解释得了、别的维度解释不了的那部分功劳。

3. **maxT 置换检验**（140 轮）把判断随机打乱重新拟合，
   取每轮所有维度里最大的效应当零分布 —— 一次性控制 6 个维度的多重比较。
   观测与置换必须用同样的迭代数（400），否则零分布系统性偏低、p 值虚小。

4. **z 标准化**：拿各维度自己的零分布算 z 分数。
   4 个取值的维度每格样本只有 2 个取值维度的一半，岭回归对小格压得更狠；
   不标准化的话单一效应量阈值会系统性漏掉多取值维度（实测学校检出 0/8）。

## 阈值

`ZMIN = 2.8`，`p ≤ 0.20`。取值扫描结果（40 次真随机乱滑 + 48 次已知真偏好）：

| z≥ | 假阳性 | 检出力 | 学校预警真阳 | 学校预警假阳 |
|---|---|---|---|---|
| 2.4 | 18% | 100% | 8/8 | 0/40 |
| 2.6 | 13% | 100% | 6/8 | 0/40 |
| **2.8** | **8%** | **100%** | **8/8**\* | **0/40** |
| 3.0 | 3% | 96% | 4/8 | 0/40 |
| 3.2 | 3% | 92% | 3/8 | 0/40 |

\* 学校预警用绝对阈值 z≥2.8。

逐项检出（每项 8 轮，不同赛道 + 不同样本量 20~34）：
要能从零搭 8/8 · 要拿得出产出 8/8 · 要同赛道 8/8 · 要竞品出身 8/8 · 要 CS 科班 8/8 · 偏好 985 以上 8/8

## 已知边界

- **n < 20 结论不可靠。** 出画像按钮 10 张解锁，但界面一直提示滑到 24 张。
- **正负例都要够。** 如果几乎全否（例如 24 张里只有 7 个正例），
  哪怕偏好是完美可分的，z 也上不去 —— 这是真实的信息量不足，不是模型问题。
- **假阳性 8%** 意味着大约每 12 次纯随机滑卡会冒出一条假结论。
  每条结论都标了「碰巧出现的概率」，别把标着"方向对，再滑一轮更稳"的当结论用。

---

## 简历抽取器（自由文本 → 维度向量）

拿 432 张卡的真实维度当 ground truth，把卡渲染成纯文本简历再抽回来：

| 维度 | 准确率 | 修之前 | 当初错在哪 |
|---|---|---|---|
| 实习公司类型 | **100%** | 44% | 独角兽名单和竞品名单有重叠，全被读成「竞品」 |
| 专业背景 | **100%** | 98% | — |
| 产出可验证性 | **100%** | 68% | `从 0 到 1` 被数字模式当成了量化结果 |
| 0→1 经历 | **98%** | 66% | 项目栏的「独立完成」被当成 0→1 证据 148 次 |
| 学校背景 | **96%** | 96% | 部分海外校名没进词表 |
| 赛道匹配 | **77%** | 33% | 简历上没人写「我在做 AI 应用」，只能从做过的模块和看的指标反推 |

修法不是加正则，是换思路：
- 公司类型从**生成器用的同一份常量**推导映射表，只扫「主实习那一行」
- 赛道用**模块 + 指标词表打分**取 argmax，公司名不参与（已由公司维度承担）
- 0→1 只认强信号（从0到1 / 冷启动 / 第一版 / 自己写PRD / 没有专职），不认「独立完成」

生产上这一层该换成 LLM 抽取，下游打分逻辑不用改 —— 它只吃维度向量。

## 硬性条件为什么是「重建」而不是「过滤」

过滤会破坏正交设计：985 + 英语可工作 + Python 三条一叠，432 张只剩 10 张，
而且剩下的这 10 张在 5 个偏好维度上严重不均衡，回归就废了。

所以硬性条件的作用是**定义候选人宇宙**：按条件重新生成整副牌，
每张卡都天然满足条件，216 个正交组合一个不少。实测筛选后仍为 432 张 / 216 组合。

真实通过率（「100 个人里只剩几个」）另外算 —— 拿未筛选的参考池 `deck0` 统计，
这个数字才是给面试官看的代价。
